apiVersion: v1
kind: Service
metadata:
  name: {{ template "azure-nodejs.fullname" . }}
  namespace: {{ template "azurenodejs-.name" . }}
  labels:
    app: {{ template "azure-nodejs.name" . }}
    chart: {{ template "azure-nodejs.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "azure-nodejs.name" . }}
    release: {{ .Release.Name }}
